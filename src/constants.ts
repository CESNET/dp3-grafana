import pluginJson from './plugin.json';

/**
 * Base URL of this plugin
 * @type {String}
 */
export const PLUGIN_BASE_URL = `/a/${pluginJson.id}`;

/**
 * ID of datasource plugin for DP3
 * @type {String}
 */
export const PLUGIN_DATASOURCE_ID = 'cesnet-dp3-datasource';
